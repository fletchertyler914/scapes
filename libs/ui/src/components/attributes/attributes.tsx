import './attributes.module.scss';

/* eslint-disable-next-line */
export interface AttributesProps {}

export const Attributes = (props: AttributesProps) => (
  <div>
    <h1>Welcome to Attributes!</h1>
  </div>
);

export default Attributes;
