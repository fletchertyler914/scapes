import './element.module.scss';

/* eslint-disable-next-line */
export interface ElementProps {}

export const Element = (props: ElementProps) => (
  <div>
    <h1>Welcome to Element!</h1>
  </div>
);

export default Element;
