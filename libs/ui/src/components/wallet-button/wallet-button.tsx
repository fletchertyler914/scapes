import './wallet-button.module.scss';

/* eslint-disable-next-line */
export interface WalletButtonProps {}

export const WalletButton = (props: WalletButtonProps) => (
  <button>Connect Wallet</button>
);

export default WalletButton;
